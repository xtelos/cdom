built
node_modules

# tsc intermediates. dist/cdom.min.js and dist/cdom.d.ts are committed: the bundle is
# what cas vendors, and the declarations are what package.json's "types" field points at.
dist/cdom.js
dist/cdom.js.map
